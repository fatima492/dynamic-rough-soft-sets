numpy>=1.21
pandas>=1.3
matplotlib>=3.4
seaborn>=0.11
scipy>=1.7
scikit-learn>=1.0

# Optional: enables the true GRU in baseline_B6_gru().  Without it the function
# falls back to an MLP and reports the substitution rather than mislabelling it.
# torch>=1.12
